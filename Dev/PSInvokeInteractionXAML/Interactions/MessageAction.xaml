<Border Name="WrapperForDEV" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Width="200" Background="White" Padding="8">
    <Border.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="..\Styles.xaml" />
                <ResourceDictionary Source="..\Icon_DEV-ONLY.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Border.Resources>
    <Border.DataContext>
        <!--DUMMY - Tag property is used to set primary style-->
        <x:Array Type="Button">
            <Button Content="Ok" Tag="True"/>
            <Button Content="Cancel" Tag="False"/>
        </x:Array>
    </Border.DataContext>
    <DockPanel>
        <!--Button panel-->
        <ListBox x:Name="LBX_Buttons" BorderThickness="0"
                 SelectionMode="Single" ItemsSource="{Binding}" DockPanel.Dock="Bottom"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled" FontSize="{DynamicResource FontSize}"
                 BorderBrush="{DynamicResource BorderBrush.S.Default}">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel FlowDirection="RightToLeft"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Button Name="PART_Btn" Content="{Binding Content}"
                                        Style="{DynamicResource ST_BTN_Secondary}"
                                        Margin="0,0,8,0"/>
                                <ControlTemplate.Triggers>
                                    <DataTrigger Binding="{Binding Tag}" Value="True">
                                        <Setter TargetName="PART_Btn" Property="Style" Value="{DynamicResource ST_BTN_Primary}"/>
                                    </DataTrigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
        <!--Message-->
        <Grid Margin="0,0,0,8">
            <DockPanel>
                <Canvas Name="CVS_Icon" Style="{DynamicResource ST_Icon}" Margin="0,0,12,0" Width="32" Height="32" DockPanel.Dock="Left"/>
                <TextBlock Name="TBK_Message" TextWrapping="Wrap" TextAlignment="Left" VerticalAlignment="Center" HorizontalAlignment="Center" 
                           Text="Message" FontSize="13"/>
            </DockPanel>
        </Grid>
    </DockPanel>
</Border>